# Suggested Commands

## Running the app
```bash
cd backend
python main.py
# OR
uvicorn main:app --host 0.0.0.0 --port 8000
```

## System commands (Windows)
- `dir` instead of `ls`
- `type` instead of `cat`
- `findstr` instead of `grep`
- Git available as usual
